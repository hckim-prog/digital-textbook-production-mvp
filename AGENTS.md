# 작업 원칙

- 사용자와 한국어로 소통하고, 변경 전에 관련 코드와 실제 데이터 흐름을 확인합니다.
- 현재 상태와 다음 작업은 `docs/NEXT_SESSION.md`를 읽습니다. 제안 단계의 기능을 구현 완료로 설명하지 않습니다.
- 원본 DOCX와 `.env`를 덮어쓰지 않습니다. 원고의 코드·숫자·표·그림·링크와 순서를 보존합니다.
- 코드 영역은 AI 교정 대상에서 제외하고 줄바꿈·탭·공백·빈 줄을 정확히 대조합니다. 공백 정규화로 불일치를 숨기지 않습니다.
- AI 제안은 사람의 승인 후 적용합니다. 기존 승인·거절·보류 기록을 임의로 변경하지 않습니다.
- 선택한 AI 모델을 다른 모델로 자동 대체하지 않습니다. 실제 API 호출은 사용자가 허용한 범위에서만 실행합니다.
- API 키, 실제 원고, 참고 PDF, 검토 기록, 결과물, 회의 문서, 로컬 백업을 공개 저장소에 추가하지 않습니다. 비밀 값은 출력하지 않습니다.
- Windows 데스크톱 프로그램입니다. 화면과 작업 스레드의 수명을 안전하게 관리하고, 실패를 정상 완료로 표시하지 않습니다.
- 첫 테스트 전에 `reports` 폴더를 만듭니다. 테스트는 `python -m pytest -q -p no:cacheprovider --basetemp reports/test-new-run`으로 실행합니다. 매번 사용하지 않은 임시 폴더를 지정합니다.
- `python main.py --verify-review-flow`는 별도 시험 원고와 고정 응답으로 순차 검토와 출력을 확인하며 유료 API를 호출하지 않습니다.
- 현재 원고 구조 분석 기능은 제안 단계입니다. 사용자 지시를 받은 뒤 구현하며, 기존 구조와 원문 보존을 우선합니다.
